name: Update Wiki

on:
  push:
    branches:
      - main
    paths:
      - 'src/**'
      - 'scripts/generate-wiki.ts'
      - 'scripts/generate-screenshots.ts'
      - 'README.md'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-wiki:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Checkout wiki
        uses: actions/checkout@v4
        with:
          repository: ${{ github.repository }}.wiki
          path: wiki-repo
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Deno
        uses: denoland/setup-deno@v1
        with:
          deno-version: v1.x

      - name: Generate wiki pages
        run: |
          echo "📝 Generating wiki documentation..."
          deno run --allow-read --allow-write scripts/generate-wiki.ts

      - name: Copy wiki pages to wiki repository
        run: |
          echo "📦 Copying generated content..."
          mkdir -p wiki-repo
          cp wiki/*.md wiki-repo/ 2>/dev/null || echo "No wiki pages found"
          
          cd wiki-repo
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Commit and push wiki changes
        working-directory: wiki-repo
        run: |
          git add .
          if git diff --staged --quiet; then
            echo "📭 No changes to wiki"
          else
            git commit -m "📚 Auto-update wiki from source code

            Generated from commit: ${{ github.sha }}
            Workflow: ${{ github.workflow }}
            Run: ${{ github.run_number }}
            
            - Updated documentation pages
            - Refreshed component screenshots
            - Synced with latest source code"
            git push
            echo "✅ Wiki updated successfully"
          fi

      - name: Summary
        run: |
          echo "## 📚 Wiki Update Complete" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Wiki has been automatically generated and updated." >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "🔗 [View Wiki](https://github.com/${{ github.repository }}/wiki)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Generated Pages" >> $GITHUB_STEP_SUMMARY
          echo "- 📖 Home - Overview and quick start" >> $GITHUB_STEP_SUMMARY
          echo "- 🎨 Components - UI elements with screenshots" >> $GITHUB_STEP_SUMMARY
          echo "- 📊 Charts - Data visualization components" >> $GITHUB_STEP_SUMMARY
          echo "- 🎨 Theme - Customization guide" >> $GITHUB_STEP_SUMMARY
          echo "- 💡 Examples - Real-world usage" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "📸 Screenshots are generated via pre-commit hook when source files change" >> $GITHUB_STEP_SUMMARY
